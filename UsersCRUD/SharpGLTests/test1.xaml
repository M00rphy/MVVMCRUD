<Window x:Class="UsersCRUD.SharpGLTests.test1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UsersCRUD.SharpGLTests"
        mc:Ignorable="d"
        Title="test1" Height="450" Width="800">
    <Grid>
        <!-- Viewport for rendering the 3D model -->
        <Viewport3D Name="MainViewport" ClipToBounds="True">
            <!-- Camera -->
            <Viewport3D.Camera>
                <PerspectiveCamera Position="0,0,5" LookDirection="0,0,-1" UpDirection="0,1,0" FieldOfView="45" />
            </Viewport3D.Camera>

            <!-- Lights -->
            <ModelVisual3D>
                <ModelVisual3D.Content>
                    <AmbientLight Color="White" />
                </ModelVisual3D.Content>
            </ModelVisual3D>
        </Viewport3D>

        <!-- Button to load the model -->
        <Button Content="Load Model" Width="100" Height="30" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10"
                Click="LoadModel_Click" />
    </Grid>
</Window>
